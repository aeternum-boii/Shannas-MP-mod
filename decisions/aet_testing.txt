country_decisions = {
	vassal_tax_test = {
		potential = {
			aet_is_debug = yes 
		}
		
		allow = { # UI I guess 
			aet_is_debug = yes 
		}
		
		effect = {
			add_country_modifier = {
				name = vassal_tax_test
				duration = -1
			}
		}
	}
	
	rural_goods_test = {
		potential = {
			aet_is_debug = yes 
		}
		
		allow = { # UI I guess 
			aet_is_debug = yes 
		}
		
		effect = {
			add_country_modifier = {
				name = rural_goods_test
				duration = -1
			}
		}
	}
	
	urban_goods_test = {
		potential = {
			aet_is_debug = yes 
		}
		
		allow = { # UI I guess 
			aet_is_debug = yes 
		}
		
		effect = {
			add_country_modifier = {
				name = urban_goods_test
				duration = -1
			}
		}
	}
	
	mining_goods_test = {
		potential = {
			aet_is_debug = yes 
		}
		
		allow = { # UI I guess 
			aet_is_debug = yes 
		}
		
		effect = {
			add_country_modifier = {
				name = mining_goods_test
				duration = -1
			}
		}
	}
	
	plantage_goods_test = {
		potential = {
			aet_is_debug = yes 
		}
		
		allow = { # UI I guess 
			aet_is_debug = yes 
		}
		
		effect = {
			add_country_modifier = {
				name = plantage_goods_test
				duration = -1
			}
		}
	}

	guard_trigger_test = {
		potential = {
			aet_is_debug = yes
		}

		allow = {
			aet_is_debug = yes
		}

		effect = {
			set_variable = {
				which = aet_temp
				value = 20
			}

			aet_applyVar = {
				left = log
				var = aet_temp
			}
		}
	}
	guard_trigger_test_1 = {
		potential = {
			aet_is_debug = yes
		}

		allow = {
			aet_is_debug = yes
		}

		effect = {
			test = yes 
		}
	}	
}